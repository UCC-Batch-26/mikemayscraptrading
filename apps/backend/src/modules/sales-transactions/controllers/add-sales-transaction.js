import { salesTransaction } from '#modules/sales-transactions/models/sales-transactions.js';
import { log } from '#utils/log.js';

export async function addSalesTransaction(req, res) {
  try {
    const { items } = req.body;
    const 
  }
}